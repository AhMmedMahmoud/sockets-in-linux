# sockets-in-linux
This is  POSIX network socket library over TCP/IP or UDP/IP for Liunx.

# build instructions
```cmake
if [ -d "build" ]; then
    rm -r build
fi

mkdir build
cd build
cmake ..
make
```
